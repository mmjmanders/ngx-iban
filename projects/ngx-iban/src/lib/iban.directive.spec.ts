import { IbanDirective } from "./iban.directive";

describe("IbanDirective", () => {
  it("should create an instance", () => {
    const directive = new IbanDirective();
    expect(directive).toBeTruthy();
  });
});
